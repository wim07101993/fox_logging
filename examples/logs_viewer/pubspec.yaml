name: log_viewer
description: An application which can display logs
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  file_picker: ">=4.0.0 <5.0.0"
  flutter:
    sdk: flutter
  flutter_fox_logging:
    path: ../../packages/flutter_fox_logging
  fox_logging:
  provider: ">=6.0.0 <7.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint:

flutter:
  uses-material-design: true
